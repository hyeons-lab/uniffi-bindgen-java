[package]
name = "uniffi-fixture-string-map"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]
name = "uniffi_fixture_string_map"

[dependencies]
uniffi = { git = "https://github.com/mozilla/uniffi-rs.git", tag = "v0.31.0" }
